/* 
  SHIM DE COMPATIBILIDAD — Este archivo redirige a la nueva ubicación.
  Re-exporta desde core/utils/domain.js para no romper imports existentes.
*/
export { getBaseDomain, getApiUrl } from 'core/utils/domain';
